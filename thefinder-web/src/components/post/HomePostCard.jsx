import { XCircle } from 'lucide-react';
export default function HomePostCard({ post, onClose, onFound }) {
  return <article className="relative grid min-h-[376px] grid-cols-1 gap-5 overflow-hidden rounded-[40px] border border-[#237596] bg-white p-3 md:grid-cols-[396px_1fr]" aria-labelledby={`post-${post.id}`}><div className="min-h-[240px] overflow-hidden rounded-[30px] bg-[#d9d9d9] md:min-h-[354px]" role="img" aria-label={`Ảnh minh họa: ${post.title}`}>{post.imageUrl && <img src={post.imageUrl} alt="" className="h-full w-full object-cover" />}</div><div className="flex min-w-0 flex-col px-2 pt-4 md:px-0 md:pr-3"><div className="flex items-start gap-2 pr-6"><span className="grid h-12 w-12 shrink-0 place-items-center rounded-full bg-[#237596] font-bold text-white">TF</span><h2 id={`post-${post.id}`} className="text-3xl text-black md:text-4xl">{post.title}</h2></div><section className="mt-3 min-h-[174px] rounded-[25px] bg-[#d9d9d9] p-4" aria-label="Nội dung bài đăng"><p className="text-[15px] font-medium text-[#1882ac]">#{post.tag || 'Chưa phân loại'}</p><p className="mt-3 text-[15px] text-black">{post.description || 'Chưa có mô tả chi tiết.'}</p></section><button type="button" className="mt-1 self-end text-[15px] text-[#237596]">Xem thêm ...</button><p className="text-xl text-black">{post.author || 'Ẩn danh'}</p><div className="mt-3 flex flex-wrap items-center gap-2 text-xl"><span>Trạng thái:</span><span className="text-[#237596]">{post.status}</span></div><button type="button" onClick={() => onFound?.(post)} className="mt-auto h-[45px] w-[241px] self-end rounded-[30px] bg-[#237596] text-xl text-white">Bạn có tìm thấy?</button></div><button type="button" onClick={() => onClose?.(post.id)} aria-label={`Đóng ${post.title}`} className="absolute right-4 top-4 text-[#d60000]"><XCircle size={22} fill="#d60000" color="white" /></button></article>;
}
